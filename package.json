{
  "name": "comment-test-coverage",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^2.0.0"
  },
  "devDependencies": {
    "@zeit/ncc": "0.20.5"
  }
}
